import 'package:flutter_test/flutter_test.dart';

void main() {
  group('AddTaskViewmodelTest -', () {
    group('addTask -', () {
      test('When called should call repo service with task and navigate back.',
          () {});
    });
  });
}
